//simple intrest
#include <stdio.h>
int main() {
    int p,t,r,result;
    printf("enter the value of p,t and r:");
    scanf("%d%d%d",&p,&t,&r);
    result=(p*t*r)/100;
        printf("the result is %d",result);
}
